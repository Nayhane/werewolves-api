const players = require('./players')
const users = require('./users')
const sessions = require('./sessions')
const villagePlayers = require('./villages/players')


module.exports = {
  players,
  users,
  sessions,
  villagePlayers
}
